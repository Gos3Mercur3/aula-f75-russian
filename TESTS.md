# Что проверяет `tools/check_repo.py`

25/25 тестов пройдено (последний прогон).

| # | Тест | Результат | Что именно проверено |
|---|---|---|---|
| 1 | `файл text_ru.xml существует и это валидный XML` | ✅ | 23302 байт |
| 2 | `кодировка UTF-16 LE + BOM + CRLF (как требует драйвер)` | ✅ | UTF-16 LE, BOM ff fe, CRLF |
| 3 | `тегов ровно 277, структура 1:1 с эталонной EN-сборкой (без дублей)` | ✅ | 277 тегов, порядок = эталон минус вставленный дубль-блок |
| 4 | `вариант text_ru_full.xml повторяет EN дословно, включая 10 дублей (287 тегов)` | ✅ | 287 = 277 + 10 дублей tc_yun1..10, порядок = эталон |
| 5 | `ни один тег не потерян и ни одна пустая строка не «оживила» перевод` | ✅ | 7 пустых совпадают: tc_cmd10, tc_kb_led21, tc_kb_led22, tc_ms24, tc_music10, tc_web, tc_yun48 |
| 6 | `переведено 270 строк, иероглифов не осталось` | ✅ | 270 строк, CJK: 0 |
| 7 | `внутри тегов нет «голого» < > & (только экранированные сущности)` | ✅ | экранирование чистое |
| 8 | `многострочный диалог прошивки остался многострочным (физический CRLF, не сущность)` | ✅ | в tc_update1 2 строки |
| 9 | `ключевые подписи не режутся панелью: укоротыши на месте` | ✅ | tc_macro_msg9:15, tc_kb5:7, tc_kb8:4, tc_kb9:9, tc_msg18:17 |
| 10 | `названия клавиш остались латиницей, единицы не переведены` | ✅ | FN+1..FN+5, ms/с, DPI, Win — как на кейкапах |
| 11 | `таблица-справочник покрывает все теги перевода` | ✅ | 277 строк, покрытие 100% |
| 12 | `батники: кодировка OEM-866 читается без потери символов, есть chcp 866` | ✅ | cp866 + chcp 866, потерь нет |
| 13 | `батники: все метки :label существуют, висячих goto/call нет` | ✅ | метки/переходы замкнуты |
| 14 | `батники: блоки if ( ... ) сбалансированы, многострочных for..in() нет` | ✅ | все блоки закрыты, многострочных for..in() нет |
| 15 | `батники: %% для циклов, отложенное расширение там, где нужно` | ✅ | %%, delayed expansion, %~1 — ок |
| 16 | `батник безопасности: нет удаления файлов, пишет только в Text\` | ✅ | никаких del/rd, запись только в Text\*\text.xml |
| 17 | `батник делает бэкап ДО записи и не затирает существующий бэкап` | ✅ | сначала „if not exist … .bak“, потом запись |
| 18 | `симуляция логики батника: установка во все языки, бэкапы, верный размер` | ✅ | en/sc/Korean: бэкап = оригинал, перевод записан, размеры совпали |
| 19 | `симуляция откатa: из .bak возвращается именно оригинал` | ✅ | оригинал восстановлен байт в байт, .bak удалён переносом |
| 20 | `повторный запуск батника не затирает бэкап (можно переустанавливать много раз)` | ✅ | 3 прогона — бэкап остался оригиналом |
| 21 | `если у пользователя нет подпапок языка — батник пишет в Text\text.xml` | ✅ | ветка exists и она рабочая |
| 22 | `архив содержит ровно то, что обещано, и ничего лишнего` | ✅ | 9 файлов, все в папке AULA_F75_RU/, CRC ok |
| 23 | `батник находит перевод при распаковке «как есть» (папка AULA_F75_RU)` | ✅ | обе разкладки (рядом / внутри AULA_F75_RU) покрыты |
| 24 | `в доках нет честных невыполнимых обещаний и битых размеров` | ✅ | размеры в доках совпадают с фактом, завышенных обещаний нет |
| 25 | `все относительные пути из README существуют после распаковки` | ✅ | ссылки на файлы валидны |

## Чего тесты НЕ покрывают

cmd.exe здесь запустить нечем (нет Windows), поэтому `УСТАНОВИТЬ.bat` проверен
статически + логика его циклов воспроизведена на фиктивной папке `Text\en\…`.
Живой двойной клик, UAC и поведение `OemDrv.exe` — проверить можно только на Windows.
